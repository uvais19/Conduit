# Google Stitch MCP — copy into your environment before starting Cursor, or merge into .env.local
# and ensure your shell / Cursor loads it (see AGENTS.md).
#
# Create a key: https://stitch.withgoogle.com → profile → Stitch settings → API key
STITCH_API_KEY=
